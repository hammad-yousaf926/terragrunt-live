name: Digger Workflow

on:
  workflow_dispatch:
    inputs:
      spec:
        description: "Digger spec file"
        required: true
      run_name:
        description: "Optional run name"
        required: false

run-name: '${{ inputs.run_name }}'

jobs:
  digger-job:
    runs-on: ubuntu-latest
    permissions:
      contents: write      # Required to merge PRs
      id-token: write      # Required for OIDC authentication
      pull-requests: write # Required to post PR comments
      issues: read         # Required to check if PR number is an issue or not
      statuses: write      # Required to validate combined PR status

    steps:
      - uses: actions/checkout@v4

      - name: Parse Project Directory
        id: parse-project-dir
        run: |
          # Extract projectDir from the JSON-like spec input
          PROJECT_DIR=$(echo '${{ github.event.inputs.spec }}' | grep -oP '(?<="projectDir":")[^"]+')
          echo "project_dir=${PROJECT_DIR}" >> $GITHUB_ENV

      - name: Debug Project Directory
        run: |
          echo "Project directory: ${{ env.project_dir }}"

      - name: Parse Account ID from account.hcl
        id: parse-account
        run: |
          ACCOUNT_ID=$(grep -oP '(?<=account_id = )\d+' ${{ env.project_dir }}/account.hcl)
          echo "account_id=${ACCOUNT_ID}" >> $GITHUB_ENV


      - name: Digger Run
        uses: diggerhq/digger@vLatest
        with:
          digger-spec: ${{ inputs.spec }}
          setup-aws: true
          aws-role-to-assume: arn:aws:iam::${{ env.account_id }}:role/digger
          aws-region: us-east-1
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
